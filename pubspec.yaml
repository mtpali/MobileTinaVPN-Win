name: mobiletina_vpn
description: MobileTinaVPN portable Windows client.
publish_to: none
version: 0.5.0+5

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  cryptography: ^2.7.0
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/protected/
